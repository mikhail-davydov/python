# магические (dunder-) методы: создание, уничтожение, строковое представление, сравнение, арифметика, вызов, преобразования
'----------------------'
# создание и инициализация
__new__(cls, *args, **kw)      # создает ПУСТОЙ объект; вернуть instance
__init__(self, *args, **kw)    # инициализирует уже созданный объект
# • оба получают те же аргументы, что и вызов класса
# • для синглтона: в __new__ хранить/возвращать cls._instance
'----------------------'
# финализация
__del__(self)                  # вызывается, когда refcount → 0; освободить ресурсы
# del имя ↓ уменьшает счётчик ссылок; метод не гарантирован при завершении интерпретатора
'----------------------'
# строковое представление
__str__(self)    # человекочитаемый (print / str / format)
__repr__(self)   # формальный, eval-friendly (repr, коллекции, интерактив)
# если __str__ не задан, используется __repr__; наоборот — нет
'----------------------'
# сравнение
__eq__, __ne__                 # ==  / !=   (возвращать True / False или NotImplemented)
__lt__, __le__, __gt__, __ge__ # <  <=  >  >=
from functools import total_ordering  # декоратор: реализуй __eq__ + один оператор ⇒ остальные
# NotImplemented → Python пробует отражённый метод rhs или даёт False / TypeError
'----------------------'
# унарные операции и функции
__pos__()     # +obj
__neg__()     # -obj
__invert__()  # ~obj
__abs__()     # abs(obj)
__round__(n)  # round(obj,n)
__trunc__()   # math.trunc
__floor__()   # math.floor
__ceil__()    # math.ceil
'----------------------'
# арифметика
__add__  __sub__  __mul__  __truediv__  __floordiv__  __mod__    # + - * / // %
__radd__ …                     # отражённые (число + obj)
__iadd__ …                     # inplace (+=, изменять или вернуть новое)
# для **  → __pow__,  divmod() → __divmod__, bit-ops → __lshift__, __and__ …
'----------------------'
# вызываемые объекты
__call__(self, *args, **kw)    # obj(...) → вызываемый экземпляр (замыкания, декораторы)
'----------------------'
# логическое значение
__bool__(self) → True/False    # bool(obj), if obj …; при отсутствии → __len__() > 0
'----------------------'
# числовые преобразования
__int__(self)      # int(obj)          __float__(self)    # float(obj)
__complex__(self)  # complex(obj)      __index__(self)    # целое для bin/hex/oct
'----------------------'
# общие рекомендации
# • __new__ должен вернуть instance текущего класса, иначе __init__ не вызовется
# • __init__ обязателен к возврату None
# • методы сравнения / арифметики при неизвестных типах → return NotImplemented
# • property / @classmethod / @staticmethod помогают скрыть внутреннее и разгрузить __init__