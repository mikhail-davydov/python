from collections import namedtuple # импорт namedtuple из модуля collections
'----------------------'
переменная = namedtuple('Тип', [имена_полей]) # объявляем тип и структуру с данными именованного кортежа
именованный_кортеж = переменная(значения_имен) # создаем именованный кортеж
именованный_кортеж[индекс] # обращение к значению по индексу
именованный_кортеж.имя_поля # обращение к значению по имени
'----------------------'
namedtuple(typename, field_names, *, rename=False, defaults=None, module=None) # Функция namedtuple
typename #  имя создаваемого типа. (обязательный параметр)
field_names # любой итерируемый объект, в том числе результат вызова map, filter. (обязательный параметр)
rename=True # неудачные имена полей (служебные слова и т.д.) переименовываются в соответствии с их порядковыми номерами, перед порядковым номером используется символ подчеркивания.
переменная = namedtuple('Тип', ['x', 'y'], defaults=(0, 0)) # задаем значения полей именованного кортежа по умолчанию (не обязательно для всех, тогда для остальных будет None).
переменная = namedtuple('Тип', [имена_полей], module='название') # атрибуту `.__ module__` результирующего именованного кортежа будет присвоено это название. Пример <class 'название.Тип'>
'----------------------'
переменная1, переменная2,.. = именованный_кортеж # распаковка именованного кортежа по переменным. Количества должны совпадать.
*именованный_кортеж # распаковка именованного кортежа
'----------------------'
именованный_кортеж:] # возможно использование срезов
'----------------------'
именованный_кортеж._fields # возвращает кортеж с именами полей
пермененная_типа_именованный_кортеж._fields # возвращает кортеж с именами полей
'----------------------'
переменная = namedtuple('Тип', [имена_полей])

именованный_кортеж = namedtuple('Тип', [*переменная._fields, 'новые_имена']) # использование в качестве имён именованного кортежа, уже существующие имена друого именованного кортежа (или переменной типа именованного кортежа)

for поле, значение in zip(переменная._fields, именованный_кортеж):
	код # перебор полей и их значений с помощью zip
'----------------------'
именованный_кортеж._field_defaults # возвращает словарь с полями и значениями именованного кортежа, если поля имеют значения по умолчанию. Если их нет, то пустой словарь.
пермененная_типа_именованный_кортеж_field_defaults # возвращает словарь с полями и значениями именованного кортежа, если поля имеют значения по умолчанию. Если их нет, то пустой словарь.
'----------------------'
именованный_кортеж.count(значение) # количество элементов в кортеже с указанным значением
именованный_кортеж.index(значение, индекс_старт, индекс_стоп) # индекс первого найденного элемента с указанным значением. Необязательные параметры: начало поиска и конец поиска.
'----------------------'
переменная = namedtuple('Тип', [имена_полей])
именованный_кортеж = переменная._make(итерируемый_объект) # использование итерируемого объекта при создании именованного кортежа. Количество элементов итерируемого объекта должно совпадать с количество имен полей, в том числе имен с значениями по умолчанию.
'----------------------'
именованный_кортеж._asdict() # возвращает словарь, ключи - имена полей, значения - значения полей. Ключи словаря имеют тот же порядок, что и поля в исходном именованном кортеже.
'----------------------'
новый_именованный_кортеж = именованный_кортеж._replace(имя_поля=нов_знач, ..) #  создание нового именованного кортежа на основании уже существующего с заменой некоторых значений